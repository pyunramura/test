---
version: '3.7'
services:
  test:
    image: containous/whoami:latest
    container_name: test1
    env:
      - "FOO=bar"
      - "BAR=baz"
    volumes:
      - "TEST:/test1"
    ports:
      - "81:80"
    networks:
      - default
volumes:
  TEST:
networks:
  default:
