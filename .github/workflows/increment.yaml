---
name: Increment Symver

on:
  push:
    branches: [ main ]

jobs:
  syncheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: ${{ github.head_ref }}
          fetch_depth: 0
      - name: Increment Symver
        uses: codacy/git-version@2.4.0